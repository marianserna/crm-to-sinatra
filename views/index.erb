<h1>Welcome to my CRM</h1>

<nav>
  <ul>
    <li>
      <a href="/">Home</a>
    </li>
    <li>
      <a href="/contacts">Add a contact</a>
    </li>
    <li>
      <a href="/about">About</a>
    </li>
  </ul>
</nav>

<!-- <section>
  <div class="total_contacts">
    <p>There are <strong><%= Contact.all.length %></strong> contacts</p>
  </div>

  <ul>
    <% @contacts.each do |contact| %>
      <li class="contact">
        <a href="/contacts/<%= contact.id %>" class="contact-info"><%= contact.first_name %>  <%= contact.last_name %> (<%= contact.email %>) </a>

        <div class="actions">
          <a href="/contacts/<%= contact.id %>/edit" class="edit-link">edit</a>
          |
          <a href="/contacts/<%= contact.id %>/delete" class="delete-link">delete</a>
        </div>
      </li>
    <% end %>
  </ul>
</section> -->
